--- /home/jvs34/Prod.DS.Revision/Benchmarks/job/queries/15a.sql
+++ queries/patched/job/15a.sql
@@ -1,6 +1,6 @@
 SELECT MIN(mi.info) AS release_date,
        MIN(t.title) AS internet_movie
-FROM aka_title AS at,
+FROM aka_title AS akat,
      company_name AS cn,
      company_type AS ct,
      info_type AS it1,
@@ -16,16 +16,16 @@
   AND mi.note LIKE '%internet%'
   AND mi.info LIKE 'USA:% 200%'
   AND t.production_year > 2000
-  AND t.id = at.movie_id
+  AND t.id = akat.movie_id
   AND t.id = mi.movie_id
   AND t.id = mk.movie_id
   AND t.id = mc.movie_id
   AND mk.movie_id = mi.movie_id
   AND mk.movie_id = mc.movie_id
-  AND mk.movie_id = at.movie_id
+  AND mk.movie_id = akat.movie_id
   AND mi.movie_id = mc.movie_id
-  AND mi.movie_id = at.movie_id
-  AND mc.movie_id = at.movie_id
+  AND mi.movie_id = akat.movie_id
+  AND mc.movie_id = akat.movie_id
   AND k.id = mk.keyword_id
   AND it1.id = mi.info_type_id
   AND cn.id = mc.company_id
--- /home/jvs34/Prod.DS.Revision/Benchmarks/job/queries/15b.sql
+++ queries/patched/job/15b.sql
@@ -1,6 +1,6 @@
 SELECT MIN(mi.info) AS release_date,
        MIN(t.title) AS youtube_movie
-FROM aka_title AS at,
+FROM aka_title AS akat,
      company_name AS cn,
      company_type AS ct,
      info_type AS it1,
@@ -17,16 +17,16 @@
   AND mi.note LIKE '%internet%'
   AND mi.info LIKE 'USA:% 200%'
   AND t.production_year BETWEEN 2005 AND 2010
-  AND t.id = at.movie_id
+  AND t.id = akat.movie_id
   AND t.id = mi.movie_id
   AND t.id = mk.movie_id
   AND t.id = mc.movie_id
   AND mk.movie_id = mi.movie_id
   AND mk.movie_id = mc.movie_id
-  AND mk.movie_id = at.movie_id
+  AND mk.movie_id = akat.movie_id
   AND mi.movie_id = mc.movie_id
-  AND mi.movie_id = at.movie_id
-  AND mc.movie_id = at.movie_id
+  AND mi.movie_id = akat.movie_id
+  AND mc.movie_id = akat.movie_id
   AND k.id = mk.keyword_id
   AND it1.id = mi.info_type_id
   AND cn.id = mc.company_id
--- /home/jvs34/Prod.DS.Revision/Benchmarks/job/queries/15c.sql
+++ queries/patched/job/15c.sql
@@ -1,6 +1,6 @@
 SELECT MIN(mi.info) AS release_date,
        MIN(t.title) AS modern_american_internet_movie
-FROM aka_title AS at,
+FROM aka_title AS akat,
      company_name AS cn,
      company_type AS ct,
      info_type AS it1,
@@ -16,16 +16,16 @@
   AND (mi.info LIKE 'USA:% 199%'
        OR mi.info LIKE 'USA:% 200%')
   AND t.production_year > 1990
-  AND t.id = at.movie_id
+  AND t.id = akat.movie_id
   AND t.id = mi.movie_id
   AND t.id = mk.movie_id
   AND t.id = mc.movie_id
   AND mk.movie_id = mi.movie_id
   AND mk.movie_id = mc.movie_id
-  AND mk.movie_id = at.movie_id
+  AND mk.movie_id = akat.movie_id
   AND mi.movie_id = mc.movie_id
-  AND mi.movie_id = at.movie_id
-  AND mc.movie_id = at.movie_id
+  AND mi.movie_id = akat.movie_id
+  AND mc.movie_id = akat.movie_id
   AND k.id = mk.keyword_id
   AND it1.id = mi.info_type_id
   AND cn.id = mc.company_id
--- /home/jvs34/Prod.DS.Revision/Benchmarks/job/queries/15d.sql
+++ queries/patched/job/15d.sql
@@ -1,6 +1,6 @@
-SELECT MIN(at.title) AS aka_title,
+SELECT MIN(akat.title) AS aka_title,
        MIN(t.title) AS internet_movie_title
-FROM aka_title AS at,
+FROM aka_title AS akat,
      company_name AS cn,
      company_type AS ct,
      info_type AS it1,
@@ -13,16 +13,16 @@
   AND it1.info = 'release dates'
   AND mi.note LIKE '%internet%'
   AND t.production_year > 1990
-  AND t.id = at.movie_id
+  AND t.id = akat.movie_id
   AND t.id = mi.movie_id
   AND t.id = mk.movie_id
   AND t.id = mc.movie_id
   AND mk.movie_id = mi.movie_id
   AND mk.movie_id = mc.movie_id
-  AND mk.movie_id = at.movie_id
+  AND mk.movie_id = akat.movie_id
   AND mi.movie_id = mc.movie_id
-  AND mi.movie_id = at.movie_id
-  AND mc.movie_id = at.movie_id
+  AND mi.movie_id = akat.movie_id
+  AND mc.movie_id = akat.movie_id
   AND k.id = mk.keyword_id
   AND it1.id = mi.info_type_id
   AND cn.id = mc.company_id
--- /home/jvs34/crossbench-sources/redbench-krid/imdb/benchmarks/job/15a.sql
+++ queries/patched/redbench_krid/15a.sql
@@ -1,6 +1,6 @@
 SELECT MIN(mi.info) AS release_date,
        MIN(t.title) AS internet_movie
-FROM aka_title AS at,
+FROM aka_title AS akat,
      company_name AS cn,
      company_type AS ct,
      info_type AS it1,
@@ -16,16 +16,16 @@
   AND mi.note LIKE '%internet%'
   AND mi.info LIKE 'USA:% 200%'
   AND t.production_year > 2000
-  AND t.id = at.movie_id
+  AND t.id = akat.movie_id
   AND t.id = mi.movie_id
   AND t.id = mk.movie_id
   AND t.id = mc.movie_id
   AND mk.movie_id = mi.movie_id
   AND mk.movie_id = mc.movie_id
-  AND mk.movie_id = at.movie_id
+  AND mk.movie_id = akat.movie_id
   AND mi.movie_id = mc.movie_id
-  AND mi.movie_id = at.movie_id
-  AND mc.movie_id = at.movie_id
+  AND mi.movie_id = akat.movie_id
+  AND mc.movie_id = akat.movie_id
   AND k.id = mk.keyword_id
   AND it1.id = mi.info_type_id
   AND cn.id = mc.company_id
--- /home/jvs34/crossbench-sources/redbench-krid/imdb/benchmarks/job/15b.sql
+++ queries/patched/redbench_krid/15b.sql
@@ -1,6 +1,6 @@
 SELECT MIN(mi.info) AS release_date,
        MIN(t.title) AS youtube_movie
-FROM aka_title AS at,
+FROM aka_title AS akat,
      company_name AS cn,
      company_type AS ct,
      info_type AS it1,
@@ -17,16 +17,16 @@
   AND mi.note LIKE '%internet%'
   AND mi.info LIKE 'USA:% 200%'
   AND t.production_year BETWEEN 2005 AND 2010
-  AND t.id = at.movie_id
+  AND t.id = akat.movie_id
   AND t.id = mi.movie_id
   AND t.id = mk.movie_id
   AND t.id = mc.movie_id
   AND mk.movie_id = mi.movie_id
   AND mk.movie_id = mc.movie_id
-  AND mk.movie_id = at.movie_id
+  AND mk.movie_id = akat.movie_id
   AND mi.movie_id = mc.movie_id
-  AND mi.movie_id = at.movie_id
-  AND mc.movie_id = at.movie_id
+  AND mi.movie_id = akat.movie_id
+  AND mc.movie_id = akat.movie_id
   AND k.id = mk.keyword_id
   AND it1.id = mi.info_type_id
   AND cn.id = mc.company_id
--- /home/jvs34/crossbench-sources/redbench-krid/imdb/benchmarks/job/15c.sql
+++ queries/patched/redbench_krid/15c.sql
@@ -1,6 +1,6 @@
 SELECT MIN(mi.info) AS release_date,
        MIN(t.title) AS modern_american_internet_movie
-FROM aka_title AS at,
+FROM aka_title AS akat,
      company_name AS cn,
      company_type AS ct,
      info_type AS it1,
@@ -16,16 +16,16 @@
   AND (mi.info LIKE 'USA:% 199%'
        OR mi.info LIKE 'USA:% 200%')
   AND t.production_year > 1990
-  AND t.id = at.movie_id
+  AND t.id = akat.movie_id
   AND t.id = mi.movie_id
   AND t.id = mk.movie_id
   AND t.id = mc.movie_id
   AND mk.movie_id = mi.movie_id
   AND mk.movie_id = mc.movie_id
-  AND mk.movie_id = at.movie_id
+  AND mk.movie_id = akat.movie_id
   AND mi.movie_id = mc.movie_id
-  AND mi.movie_id = at.movie_id
-  AND mc.movie_id = at.movie_id
+  AND mi.movie_id = akat.movie_id
+  AND mc.movie_id = akat.movie_id
   AND k.id = mk.keyword_id
   AND it1.id = mi.info_type_id
   AND cn.id = mc.company_id
--- /home/jvs34/crossbench-sources/redbench-krid/imdb/benchmarks/job/15d.sql
+++ queries/patched/redbench_krid/15d.sql
@@ -1,6 +1,6 @@
-SELECT MIN(at.title) AS aka_title,
+SELECT MIN(akat.title) AS aka_title,
        MIN(t.title) AS internet_movie_title
-FROM aka_title AS at,
+FROM aka_title AS akat,
      company_name AS cn,
      company_type AS ct,
      info_type AS it1,
@@ -13,16 +13,16 @@
   AND it1.info = 'release dates'
   AND mi.note LIKE '%internet%'
   AND t.production_year > 1990
-  AND t.id = at.movie_id
+  AND t.id = akat.movie_id
   AND t.id = mi.movie_id
   AND t.id = mk.movie_id
   AND t.id = mc.movie_id
   AND mk.movie_id = mi.movie_id
   AND mk.movie_id = mc.movie_id
-  AND mk.movie_id = at.movie_id
+  AND mk.movie_id = akat.movie_id
   AND mi.movie_id = mc.movie_id
-  AND mi.movie_id = at.movie_id
-  AND mc.movie_id = at.movie_id
+  AND mi.movie_id = akat.movie_id
+  AND mc.movie_id = akat.movie_id
   AND k.id = mk.keyword_id
   AND it1.id = mi.info_type_id
   AND cn.id = mc.company_id
